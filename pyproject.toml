[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "quantum-surface-code"
version = "0.1.0"
description = "A small, dependency-free rotated surface-code simulator"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]

